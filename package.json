{
  "name": "evictl",
  "version": "0.1.0",
  "description": "Orchestrate local evi agent runtimes and shared memory",
  "type": "module",
  "bin": {
    "evictl": "./src/cli.ts"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bun test",
    "build": "bun build ./src/cli.ts --compile --outfile ./dist/evictl"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
